import { Merchi } from '../merchi.js';
test('can make InventoryGroup', function () {
    var merchi = new Merchi();
    var inventoryGroup = new merchi.InventoryGroup();
    expect(inventoryGroup).toBeTruthy();
});
