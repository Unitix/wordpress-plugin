import { Merchi } from '../merchi.js';
test('can make DraftPreviewLayer', function () {
    var merchi = new Merchi();
    var draftPreviewLayer = new merchi.DraftPreviewLayer();
    expect(draftPreviewLayer).toBeTruthy();
});
